pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Checkout source code from Git
                checkout scm
            }
        }
        
        stage('Run Tests') {
            steps {
                // Run tests using Django's test runner
                bat 'python manage.py test'
            }
        }
    }
}
